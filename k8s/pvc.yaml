apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: autospec-vector-store-pvc
  labels:
    app: autospec-search
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
